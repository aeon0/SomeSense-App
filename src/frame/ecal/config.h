#include "interface/_gen/frame.capnp.h"

namespace config {
  const int INTERFACE_VERSION_MAJOR = INTERFACE_VERSION_MAJOR;
  const int INTERFACE_VERSION_MINOR = INTERFACE_VERSION_MINOR;
  const double GOAL_FRAME_LENGTH = 50.0; // 50 ms -> 20 fps
}
